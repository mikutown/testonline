spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/testonline?characterEncoding=utf8&amp;useSSL=false
mybatis:
  mapper-locations: classpath:/mybatis/mapper/*.xml
logging:
  level:
    top.leafii.testonline.mapper: Debug
